/// Agent Coordinator — agent presence, command dispatch, update approval.
/// Receives agent heartbeats, tracks online/offline state, dispatches commands
/// on behalf of operators, and manages update approval workflows.
package hostmgr:agent-coordinator@0.1.0;

world agent-coordinator {
    import wasmcloud:messaging/consumer@0.2.0;

    export wasmcloud:messaging/handler@0.2.0;
}
